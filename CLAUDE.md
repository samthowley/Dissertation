# Working preferences

- Do not proactively output docs or images (e.g. rendered PNG figures, prototype plots). When drafting figure/analysis code (R scripts, etc.) that produces a file, write the full code but leave the run/render/save lines (e.g. `ggsave(...)`) commented out with `#` until the user explicitly asks to run it. Don't preview generated outputs back into the conversation unless asked.
